name: cache-redis-config workflow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run a simple build step
        run: |
          echo "Building cache-redis-config project"
          echo "Project built successfully"